# Production Environment Configuration
# Copy this file to .env and update with your production values

# Server Configuration
PORT=5000
NODE_ENV=production

# Database Configuration
# Primary: MongoDB Atlas (Cloud)
MONGO_URI=mongodb+srv://superuser:superuser123@cluster0.xlgvetl.mongodb.net/?appName=Cluster0
# Backup: Local MongoDB (Fallback)
MONGO_URI_BACKUP=mongodb://localhost:27017/rice_mill

# Security
# IMPORTANT: Generate a new secure JWT secret for production
# Run: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=REPLACE_WITH_GENERATED_SECRET_FROM_COMMAND_ABOVE

# Frontend URL (for CORS)
FRONTEND_URL=https://your-production-domain.com

# Optional: Logging & Monitoring
# LOG_LEVEL=error
# SENTRY_DSN=your-sentry-dsn

# Optional: Email Configuration (for notifications)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
